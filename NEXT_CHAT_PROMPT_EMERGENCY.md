Continue Anime Deduction Tower from PR31 state. Start by reading `README.md`, `AGENTS.md`, `docs/PR31_HANDOFF.md`, and `NEXT_CHAT_PROMPT.md`; before coding also read `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, `docs/DATA_MODEL.md`, `skills/flutter-architecture/SKILL.md`, `skills/testing/SKILL.md`, and `skills/ui-ux-mobile/SKILL.md`. Current state: local no-lives gameplay works, protected one-device secrecy works, AI mode works with easy/standard/hard behavior, online room foundation works with mock + Firebase room create/join/watch/readiness, explicit bootstrap/public/private/action contracts exist, Firestore bootstrap docs persist/watch, reconnect-aware handoff and read-only remote hydration exist, queued actions + pure Dart resolution exist, canonical public online event docs now persist for future remote timeline/result UI, and Firebase backend mode also has a backend-authority bridge flow that auto-resolves pending actions with `backendService` metadata while preserving `resolvedByParticipantId`, `resolvedByUserId`, `resolutionSource`, and the canonical public event shape; room participants preserve `userId`; runtime catalog is 1276 characters / 40 tags / 40 categories. Keep game logic pure Dart, keep online transport separate from official match-rule resolution, keep widgets thin, keep Flame presentation-only, add/update tests when logic changes, and if JSON assets change remind that a full app restart is needed. Best next step: replace the current backend-authority bridge with a true backend-owned resolver path while preserving current explicit action metadata and canonical public event shape.